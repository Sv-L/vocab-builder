import styled from 'styled-components';

export const LogOutStyles = styled.button`
  margin-top: 28px;
  background: none;
  border: none;
  display: flex;
  gap: 6px;
  align-items: center;
  justify-content: center;
  color: var(--light);
  font-weight: 500;
  font-size: 14px;
  cursor: pointer;
`;
